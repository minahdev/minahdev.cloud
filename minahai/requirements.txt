# ─────────────────────────────────────────────
# 웹 서버 / API (FastAPI 백엔드 뼈대)
# ─────────────────────────────────────────────
fastapi==0.136.1            # 웹 프레임워크 (API 서버 본체)
uvicorn[standard]==0.47.0   # ASGI 서버 (FastAPI 실행기)
python-multipart==0.0.20    # 파일 업로드 / 폼 데이터 처리
httpx==0.28.1               # 비동기 HTTP 클라이언트 (외부 API 호출)
mcp>=1.0.0                  # Model Context Protocol (LLM 툴 연동)

# ─────────────────────────────────────────────
# 데이터베이스 / ORM
# ─────────────────────────────────────────────
sqlalchemy[asyncio]==2.0.49 # ORM (DB를 파이썬 객체로 다룸)
sqlmodel>=0.0.38            # SQLAlchemy + Pydantic 결합 모델
psycopg[binary]==3.3.4      # PostgreSQL 드라이버
pgvector>=0.3.0            # PostgreSQL 벡터 검색 (임베딩 저장)
alembic>=1.13.0            # DB 마이그레이션 (스키마 버전 관리)
neo4j>=5.0.0              # 그래프 DB 드라이버 (온톨로지/관계)

# ─────────────────────────────────────────────
# 인증 / 보안 / 알림
# ─────────────────────────────────────────────
bcrypt>=4.0.0             # 비밀번호 해싱
pywebpush==2.0.3          # 웹 푸시 알림 발송

# ─────────────────────────────────────────────
# 데이터 처리 / 시각화
# ─────────────────────────────────────────────
pandas==3.0.3             # 데이터프레임 (표 형태 데이터 처리)
matplotlib>=3.7.0         # 기본 그래프 그리기
seaborn>=0.13.0           # 통계 시각화 (matplotlib 확장)
plotly>=5.18.0            # 인터랙티브 차트

# ─────────────────────────────────────────────
# 머신러닝 (전통 ML — 표/수치 데이터 예측)
# ─────────────────────────────────────────────
scikit-learn==1.8.0       # ML 표준 도구 (분류/회귀/전처리)
xgboost>=2.0.0            # 그래디언트 부스팅 (캐글 강자)
lightgbm>=4.0.0          # 경량·고속 부스팅
catboost>=1.2.0          # 범주형 데이터 특화 부스팅

# ─────────────────────────────────────────────
# 딥러닝 / 비전 (CNN → YOLO 계열)
# ─────────────────────────────────────────────
torch==2.12.1            # PyTorch — 딥러닝 핵심 엔진 (YOLO 두뇌)
torchvision==0.27.1      # 이미지 모델·변환 유틸
torchaudio==2.11.0       # 오디오 처리
opencv-python>=4.9.0     # 영상/웹캠 입출력 + 바운딩박스 시각화
ultralytics>=8.2.0       # YOLO 표준 라이브러리 (객체 탐지)

# ─────────────────────────────────────────────
# LLM / 생성형 AI
# ─────────────────────────────────────────────
google-genai>=1.0.0      # Google Gemini API
transformers>=4.40.0     # HuggingFace 모델 (사전학습 모델 로드)
ollama>=0.4.0            # 로컬 LLM 실행
kiwipiepy>=0.21.0        # 한국어 형태소 분석기
langchain==0.3.25         # LLM 오케스트레이션 프레임워크
langchain-core==0.3.63    # langchain 핵심 추상화
langchain-community==0.3.24 # langchain 커뮤니티 통합
langgraph==0.2.70        # LLM 워크플로 그래프 (에이전트)
langsmith>=0.1.125,<0.4  # langchain 추적/디버깅

# ─────────────────────────────────────────────
# 클라우드 / 인프라
# ─────────────────────────────────────────────
boto3==1.35.99           # AWS SDK (S3 등)

# ─────────────────────────────────────────────
# 환경설정 / 테스트
# ─────────────────────────────────────────────
python-dotenv==1.2.2      # .env 환경변수 로드
pytest>=8.0.0            # 테스트 프레임워크
pytest-asyncio>=0.23.0   # 비동기 테스트 지원
